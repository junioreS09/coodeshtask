<Window x:Class="WpfApp.Views.PessoaDetalheView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:WpfApp.Helpers"
        mc:Ignorable="d"
        Title="Detalhe da Pessoa" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <helpers:StatusParaVisibilidadeConverter x:Key="StatusConverter"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="RowBackground" Value="#1a1a2e"/>
            <Setter Property="AlternatingRowBackground" Value="#16213e"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#0f3460"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a2e" Offset="0"/>
                <GradientStop Color="#16213e" Offset="0.5"/>
                <GradientStop Color="#0f3460" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TÍTULO -->
            <TextBlock Text="👤  Detalhe da Pessoa" FontSize="22" FontWeight="Bold"
                       Foreground="White" Grid.Row="0" Margin="0,0,0,15"/>

            <!-- DADOS DA PESSOA -->
            <Border Grid.Row="1" Background="#1a1a2e" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Nome:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,8,5"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,5" Text="{Binding Nome}"/>

                    <TextBlock Text="CPF:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,8,5"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Width="150" HorizontalAlignment="Left" Margin="0,5" Text="{Binding Cpf}"/>

                    <TextBlock Text="Endereço:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,8,5"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,5" Text="{Binding Endereco}"/>
                </Grid>
            </Border>

            <!-- BOTÕES DA PESSOA -->
            <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,15">
                <Button Content="💾  Salvar Pessoa" Width="130" Margin="0,0,8,0"
                        Background="#0f8a5f" Click="BtnSalvar_Click"/>
                <Button Content="＋  Incluir Pedido" Width="130"
                        Background="#e94560" Click="BtnIncluirPedido_Click"/>
            </StackPanel>

            <!-- FILTRO DE PEDIDOS -->
            <Border Grid.Row="3" Background="#1a1a2e" CornerRadius="6" Padding="10" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Filtrar pedidos:" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <RadioButton x:Name="RbTodos" Content="Todos" IsChecked="True"
                                 VerticalAlignment="Center" Margin="0,0,12,0" Checked="FiltroStatus_Checked"/>
                    <RadioButton x:Name="RbPendente" Content="Pendentes"
                                 VerticalAlignment="Center" Margin="0,0,12,0" Checked="FiltroStatus_Checked"/>
                    <RadioButton x:Name="RbPago" Content="Pagos"
                                 VerticalAlignment="Center" Margin="0,0,12,0" Checked="FiltroStatus_Checked"/>
                    <RadioButton x:Name="RbRecebido" Content="Entregues"
                                 VerticalAlignment="Center" Checked="FiltroStatus_Checked"/>
                </StackPanel>
            </Border>

            <!-- GRID DE PEDIDOS -->
            <DataGrid x:Name="GridPedidos" Grid.Row="4"
                      ItemsSource="{Binding Pedidos}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      ColumnHeaderHeight="36">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0f3460"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="FontSize" Value="13"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataVenda, StringFormat=dd/MM/yyyy}" Width="90"/>
                    <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotal, StringFormat=C}" Width="110"/>
                    <DataGridTextColumn Header="Pagamento" Binding="{Binding FormaPagamentoCompleto}" Width="130"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                    <DataGridTemplateColumn Header="Ações" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Pago" Width="60" Margin="2" Padding="4,3"
                                            Background="#0f8a5f" Click="BtnPago_Click"
                                            Visibility="{Binding Status, Converter={StaticResource StatusConverter}, ConverterParameter=Pendente}"/>
                                    <Button Content="Enviado" Width="68" Margin="2" Padding="4,3"
                                            Background="#e8a020" Click="BtnEnviado_Click"
                                            Visibility="{Binding Status, Converter={StaticResource StatusConverter}, ConverterParameter=Pago}"/>
                                    <Button Content="Recebido" Width="75" Margin="2" Padding="4,3"
                                            Background="#0f3460" Click="BtnRecebido_Click"
                                            Visibility="{Binding Status, Converter={StaticResource StatusConverter}, ConverterParameter=Enviado}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>
    </Grid>
</Window>
