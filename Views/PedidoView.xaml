<Window x:Class="WpfApp.Views.PedidoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Novo Pedido" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="RowBackground" Value="#1a1a2e"/>
            <Setter Property="AlternatingRowBackground" Value="#16213e"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#0f3460"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a2e" Offset="0"/>
                <GradientStop Color="#16213e" Offset="0.5"/>
                <GradientStop Color="#0f3460" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- TÍTULO -->
            <TextBlock Text="🧾  Novo Pedido" FontSize="22" FontWeight="Bold"
                       Foreground="White" Grid.Row="0" Margin="0,0,0,15"/>

            <!-- PESSOA E PAGAMENTO -->
            <Border Grid.Row="1" Background="#1a1a2e" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Pessoa:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Width="220" Margin="0,0,20,0"
                              ItemsSource="{Binding Pessoas}"
                              SelectedItem="{Binding PessoaSelecionada}"
                              IsEnabled="{Binding PessoaHabilitada}"
                              DisplayMemberPath="Nome"/>
                    <TextBlock Text="Pagamento:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="CmbPagamento" Width="150" Margin="0,0,15,0" SelectionChanged="CmbPagamento_SelectionChanged">
                        <ComboBoxItem Content="Dinheiro"/>
                        <ComboBoxItem Content="Cartão"/>
                        <ComboBoxItem Content="Boleto"/>
                    </ComboBox>
                    <TextBlock Text="Parcelas:" VerticalAlignment="Center" Margin="0,0,8,0"
                               Visibility="{Binding VisibilidadeParcelas}"/>
                    <ComboBox Width="100"
                              ItemsSource="{Binding ParcelasOpcoes}"
                              SelectedItem="{Binding ParcelaSelecionada}"
                              Visibility="{Binding VisibilidadeParcelas}"/>
                </StackPanel>
            </Border>

            <!-- ADICIONAR PRODUTO -->
            <Border Grid.Row="2" Background="#1a1a2e" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Produto:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox Width="220" Margin="0,0,15,0"
                              ItemsSource="{Binding Produtos}"
                              SelectedItem="{Binding ProdutoSelecionado}"
                              DisplayMemberPath="Nome"/>
                    <TextBlock Text="Quantidade:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="TxtQuantidade" Width="60" Margin="0,0,15,0"
                             Text="{Binding Quantidade}" TextAlignment="Center"/>
                    <Button Content="＋  Adicionar" Width="110" Background="#0f8a5f"
                            Click="BtnAdicionarItem_Click"/>
                </StackPanel>
            </Border>

            <!-- GRID DE ITENS -->
            <DataGrid x:Name="GridItens" Grid.Row="3"
                      ItemsSource="{Binding Itens}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      ColumnHeaderHeight="36"
                      Margin="0,0,0,10">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0f3460"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="FontSize" Value="13"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Produto" Binding="{Binding NomeProduto}" Width="*"/>
                    <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="90"/>
                    <DataGridTextColumn Header="Valor Unit." Binding="{Binding ValorUnitario, StringFormat=C}" Width="110"/>
                    <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" Width="110"/>
                    <DataGridTemplateColumn Header="Ação" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="🗑" Background="#e94560" Width="40"
                                        Click="BtnRemoverItem_Click"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- RODAPÉ: TOTAL E FINALIZAR -->
            <Border Grid.Row="4" Background="#1a1a2e" CornerRadius="6" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Valor Total:" FontSize="16" FontWeight="Bold" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding ValorTotal, StringFormat=C}"
                                   FontSize="18" FontWeight="Bold" Foreground="#0f8a5f"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="✔  Finalizar Pedido" Width="160" Height="36"
                            Background="#0f8a5f" FontSize="14" Click="BtnFinalizar_Click"/>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</Window>
