<Window x:Class="WpfApp.Views.PessoaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Cadastro de Pessoas" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#0f3460"/>
            <Setter Property="RowBackground" Value="#1a1a2e"/>
            <Setter Property="AlternatingRowBackground" Value="#16213e"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#0f3460"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#1a1a2e" Offset="0"/>
                <GradientStop Color="#16213e" Offset="0.5"/>
                <GradientStop Color="#0f3460" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TÍTULO -->
            <TextBlock Text="👤  Cadastro de Pessoas" FontSize="22" FontWeight="Bold"
                       Foreground="White" Grid.Row="0" Margin="0,0,0,15"/>

            <!-- FILTROS -->
            <Border Grid.Row="1" Background="#1a1a2e" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Width="180" Margin="0,0,15,0" Text="{Binding NomeFiltro, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="CPF:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox Width="140" Margin="0,0,15,0" Text="{Binding CpfFiltro, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="🔍  Buscar" Width="90" Click="BtnBuscar_Click"/>
                </StackPanel>
            </Border>

            <!-- BOTÕES DE AÇÃO -->
            <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
                <Button Content="＋  Incluir" Width="100" Margin="0,0,8,0"
                        Background="#0f8a5f" Click="BtnIncluir_Click"/>
                <Button Content="✏  Editar" Width="100" Margin="0,0,8,0"
                        Background="#e8a020" Click="BtnEditar_Click"/>
                <Button Content="🗑  Excluir" Width="100"
                        Background="#e94560" Click="BtnExcluir_Click"/>
            </StackPanel>

            <!-- GRID -->
            <DataGrid x:Name="GridPessoas" Grid.Row="3"
                      ItemsSource="{Binding Pessoas}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      ColumnHeaderHeight="36">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0f3460"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="FontSize" Value="13"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding Cpf}" Width="140"/>
                    <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
